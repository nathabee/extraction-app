<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌙 Dark Theme -->
    <style name="Theme.VisuBee" parent="Theme.Material3.Dark.NoActionBar">

        <!-- Primary Colors -->
        <item name="colorPrimary">@color/purple_200</item> <!-- Light Purple -->
        <item name="colorPrimaryVariant">@color/purple_700</item> <!-- Dark Purple -->
        <item name="colorOnPrimary">@color/black</item>

        <!-- Backgrounds & Surfaces -->
        <item name="android:windowBackground">@color/gray_900</item> <!-- Dark gray background -->
        <item name="colorSurface">@color/gray_900</item>
        <item name="colorOnSurface">@color/white</item> <!-- Ensure readability! -->

        <!-- Status Bar & Nav Bar -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/white</item>

    </style>

</resources>
