<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light Theme -->
    <style name="Theme.KotlinTestApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary App Colors -->
        <item name="colorPrimary">@color/myPrimaryColor</item>
        <item name="colorPrimaryVariant">@color/myPrimaryVariant</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Background Colors -->
        <item name="android:windowBackground">@color/myBackgroundColor</item>

        <!-- Typography -->
    </style>

    <!-- Dark Theme -->
    <style name="Theme.KotlinTestApp.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/myDarkPrimaryColor</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

</resources>
